#!/usr/bin/perl
package Config;

use strict;
use warnings;

# Proxmox API configuration
our $PROXMOX_HOST = 'your-proxmox-host';
our $PROXMOX_USER = 'root@pam';
our $PROXMOX_PASSWORD = 'your-password';
our $PROXMOX_PORT = 8006;

1;